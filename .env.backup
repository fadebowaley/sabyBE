# Local Development Environment for haloBE with Mailtrap SMTP
NODE_ENV=development
PORT=4000

# MongoDB (using local MongoDB or mock)
MONGODB_URL=mongodb://localhost:27017/halo-local

# PostgreSQL (using local PostgreSQL or mock)
POSTGRES_HOST=20.169.129.160
POSTGRES_PORT=5432
POSTGRES_USER=sabyagentic_user
POSTGRES_PASSWORD=WcKoT/m9hFGMaiztjci/reLyMVln9qE0ReAaHc0Cb8E=
POSTGRES_DB=halograph

# Redis (using local Redis or mock)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT (required)
JWT_SECRET=local-dev-secret-key-change-in-production

# Frontend URL
HALOFE_URL=http://localhost:3000

# SMTP Configuration for Mailtrap (Development)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USERNAME=8c076ad2a217ad
SMTP_PASSWORD=e961f30e412d44
EMAIL_FROM=noreply@saby.ai

# Disable telegram/whatsapp features for local testing
TELEGRAM_BOT_TOKEN=
PHONE_NUMBER_ID=
VERIFY_TOKEN=
WHATSAPP_TOKEN=
